<!DOCTYPE html>
<html lang="es">
<head>
    <% include ../partials/head %>
</head>
<body>
    <%- cabecera -%>
    <div class="container">
        <main>
            <div class="row">
                <div class="col-md-6" >
                    <p>¿Seguro que quiere eliminar la medida <%= metro.medida %>?</p>
                    <button class="btn btn-primary" data-id="<%= metro.id_medida %>">Sí</button> <button class="btn btn-warning">No</button>

                </div>

            </div>
        </main>
    </div>
    <p></p>
    <script type="text/javascript">
        var boton = $('.btn-primary')
        boton.click(function(ev) {
            ev.preventDefault()
            $.post(`/medidas/delete/${boton.data('id')}`, function(res) {
                window.location.href = '/medidas/listar'
            }, 'json')
        })
        $('.btn-warning').click(function(ev) {
            ev.preventDefault()
            history.back(1)
        })
    </script>
    <% include ../partials/footer %>
</body>
</html>
